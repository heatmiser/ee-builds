# No Python packages required for the first-boot renderer collection set.
# ansible.posix, community.crypto, and containers.podman have no pip dependencies
# beyond what is pre-installed in ee-minimal-rhel9 (AAP 2.6).
