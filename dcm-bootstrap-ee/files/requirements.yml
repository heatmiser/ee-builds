---
collections:
  # First-boot renderer requirements only — no OCP/Kubernetes capabilities here.
  # Site orchestration (kubernetes.core, dellemc.openmanage) lives in dcm-ocp-ee-4.x.

  - name: ansible.posix
    version: 2.1.0

  - name: community.crypto
    version: ">=2.0.0"

  - name: containers.podman
    version: 1.18.0

  - name: rhvp.ocp_landing_zone
    type: git
    source: https://github.com/heatmiser/rhvp.ocp_landing_zone.git
    version: 0.3.6

  - name: elise.quadlet
    type: git
    source: https://github.com/heatmiser/elise.quadlet.git
    version: 0.1.3
...
