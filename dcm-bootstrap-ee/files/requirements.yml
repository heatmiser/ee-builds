---
collections:
  # First-boot renderer requirements only — no OCP/Kubernetes capabilities here.
  # Site orchestration (kubernetes.core, dellemc.openmanage, rhvp.ocp_landing_zone)
  # lives in dcm-base-ee and dcm-ocp-ee-4.x.

  - name: ansible.posix
    version: 2.1.0

  - name: community.crypto
    version: ">=2.0.0"

  - name: containers.podman
    version: 1.18.0
...
